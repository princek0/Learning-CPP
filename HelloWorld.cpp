#include <iostream> // Preprocessor Directive, this line will be processed before compliation.

int main() // The name of a function is called an 'identifier'
{
   std::cout << "Hello world!"; // Statements must end with a ; 
   return 0; // Returning 0 indicates that the function worked properly 
}
